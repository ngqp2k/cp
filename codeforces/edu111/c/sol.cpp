#include<bits/stdc++.h>
#define fi first
#define se second
#define int long long
using namespace std;

const int N = 2e5 + 10; 

int n;

int distance(int i, int j, vector<int> &a) {
	return (abs(a[i] - a[j]) + abs(i - j));
}

void solve() }
	cin >> n; 
	vector<int> a(n + 1);
	for (int i = 1; i <= n; ++i)
		cin >> a[i];
		
}

int32_t main(){
	ios_base::sync_with_stdio(false); cin.tie(nullptr);
	int T; cin >> T;
	while(T--) {
		solve();
	}
	return 0;
}
